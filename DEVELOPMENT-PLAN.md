# DEVELOPMENT-PLAN.md

描述项目开发计划和进度的文件.

## 页面概览

- [x] 登录页
- [x] 注册页
- [x] 404 页面
- [x] 错误兜底页面
- [x] 仪表盘页
- [x] 股票详情页
- [x] 指标信号页
- [ ] 系统管理
  - [ ] 用户管理
  - [ ] 角色管理
  - [ ] 菜单管理
  - [ ] 配置管理
- [ ] 策略管理
- [ ] 回测管理

## 仪表盘页

路由: `/dashboard`

上下排列四个块: A股、港股、港股通、美股

### 布局要求

- 使用 Card 组件展示每个指数
- 市场标题使用 Tag（加大加粗），下方添加分割线
- 响应式布局（antd 栅格）:
  - xs（移动端）：1个/行
  - lg（桌面）：2个/行
  - xl（大屏）：3个/行
  - xxl（超大屏）：4个/行

### 市场板块

- **A股**: 显示`上证指数`、`深证成指`、`创业板指`、`科创综指`、`科创50`、`中证A500`、`沪深300`、`中证500`、`中证800`、`中证1000`、`上证50`、`上证180`、`深证100`
- **港股**: 显示`恒生指数`、`国企指数`、`恒生科技指数`
- **港股通**: 显示`沪股通`（`深股通`、`沪深港300` 待补充）
- **美股**: 显示`道琼斯`、`纳斯达克`、`标普500`、`纳斯达克中国金龙指数`、`纳斯达克100`

### 功能要求

- 使用 Card 展示每个指数（名称、代码、现价、涨跌幅、最高、最低、昨收）
- 涨跌幅颜色标识（红涨绿跌）
- 根据交易时间判断是否轮询:
  - A股/港股通: 周一至周五 9:30-11:30, 13:00-15:00
  - 港股: 周一至周五 9:30-12:00, 13:00-16:00
  - 美股: 周一至周五 9:30-16:00 (美东时间)
- 不在交易时间内显示休市提示及下次开盘时间
- 使用 React Query 轮询获取数据（交易时间内每5秒）
- 点击指数卡片：写入 `useStock`（持久化）后跳转到股票详情页

## 股票详情页

路由: `/stock-detail`

当前股票由 `src/zustand/useStock.ts` 管理并持久化，页面间共享 —— 不再通过 URL query 传参；可从仪表盘点击指数卡片进入，或在页内搜索框切换。

### 功能要求

- 页内搜索框（`StockPicker` → `StockSearch`）：按代码/名称搜索，输入停止 300ms 后请求（防抖），过滤基金，美股代码去除交易所后缀，选中后更新当前股票
- 行情卡片（`StockQuoteCard`）：名称/代码/市场标签、现价与涨跌、今开/最高/最低/昨收/成交量/成交额/换手率/振幅/量比/市盈率/市净率/总市值
- 交易时间内每5秒轮询行情（多页面复用同一 queryKey，共享缓存）
- 图表周期切换（Radio 按钮）：分时、日K、周K、月K、1分、5分、15分、30分、60分（五日分时逻辑保留，暂不展示）
- 图表使用 lightweight-charts：主图为蜡烛图（分时/五日的折线逻辑保留），副图为成交量柱
- 十字光标悬浮提示：日期、开高低收、涨跌幅、成交量，提示框自动避让容器边缘
- 分时图固定显示 09:30-15:30 并禁用缩放/滚动；K线默认显示最新 100 条，可见条数限制在 30-200 条（分钟K不受上限限制）
- 分时与 1 分 K 线（均为 `period=1`）不传日期，由后端自动定位目标交易日（盘前/休市取前一交易日，交易中/午休/盘后取当天）；该周期不支持复权
- 面包屑导航：QUANT > 股票详情

## 指标信号页

路由: `/stock-signals`

布局与股票详情页一致：顶部复用 `StockPicker`（搜索框 + 行情卡片），下方为信号表格。

### 功能要求

- K线周期切换（Radio 按钮）：日线、周线、月线
- 信号来源：K线技术指标（MA、MACD、KDJ、RSI、BOLL、SAR）
- 信号类型 14 种：MA/MACD/KDJ 金叉与死叉、KDJ/RSI 超买与超卖、突破布林上轨/跌破布林下轨、SAR 反转向上/向下
- 表格列：日期、信号（Tag 着色）、购买建议（红看多/绿看空/橙谨慎/青关注）、说明、收盘价、详情
- 最新信号在前，每页 20 条
- 待办：当前查询未传 `startDate`，后端会在全部历史上识别信号（数据量可能较大），后续可考虑按时间窗口收窄
- 面包屑导航：QUANT > 指标信号

## 已完成功能

### 认证系统

- JWT 双 token 方案（accessToken + refreshToken）
- accessToken 过期自动刷新
- 请求拦截器自动附加 Authorization 头（从 Zustand store 获取）
- 401 错误时自动刷新 token（并发请求进入等待队列，刷新后统一重试），刷新失败跳转登录页
- 注册表单校验与后端契约对齐（用户名 3-32 位且仅字母/数字/下划线；密码 8-64 位且含大小写字母、数字、特殊字符）

### API 层

- 统一请求封装（`src/utils/request.ts`）：baseURL 取 `VITE_BASE_URL`（未配置回退 `/api`），超时 10s；响应拦截器解包 `{ code, message, data }` 直接返回 `data`，错误统一 toast 提示
- 认证接口：`login`、`register`
- 股票接口：
  - `getStockQuotes(market, codes)` — 批量获取行情
  - `getStockKLine(market, code, period, startDate?, endDate?)` — 历史K线（日/周/月/分钟）
  - `searchStock(keyword)` — 搜索股票/指数/基金
  - `getStockSignals(market, code, period, maFast, maSlow)` — K线技术指标信号

### 公共组件

- `StockPicker` — 股票选择区：搜索框 + 当前股票行情卡片（股票详情页与指标信号页共用）
- `StockSearch` — 股票搜索框（防抖搜索、结果过滤、选中回调）
- `StockQuoteCard` — 行情详情卡片（多页面共用同一 queryKey 缓存）
- `ThemeSegmented` — 主题切换下拉

### 数据管理

- TanStack Query 全局客户端（`src/queryClient.ts`）：retry 1、关闭窗口聚焦重取、staleTime 10s
- Zustand store：`useAuth`（认证状态，持久化 key `auth`）、`useStock`（当前股票，持久化 key `stock`）
- `useTradingStatus`（`src/hooks/`）：交易时段判定与 5 秒轮询间隔，供行情类查询使用

### 主题系统

- 支持浅色/深色/系统三种模式
- 使用 next-themes 实现主题切换
- 主题感知样式（antd 算法 + CSS 变量）

### 路由结构

- `/` - 重定向到 `/dashboard`
- `/login` - 登录页
- `/register` - 注册页
- `/dashboard` - 仪表盘页
- `/stock-detail` - 股票详情页
- `/stock-signals` - 指标信号页
- `*` - 404页面
